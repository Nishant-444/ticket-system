# Server Port (Default is 8080)
PORT=8080

# Secret key for JWT signing and verification (Change this to a strong secret in production)
JWT_SECRET=super-secret-ticket-system-jwt-key-eva-bharat-2026

# SQLite Database storage location
DB_PATH=./data/tickets.db
